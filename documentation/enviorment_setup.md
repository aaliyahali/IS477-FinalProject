# Environment Setup

This project uses Python 3.10+ and a small set of scientific Python libraries.

## 1. Create and Activate a Virtual Environment

```bash
python -m venv .venv
source .venv/bin/activate        # macOS / Linux
# or on Windows:
# .venv\Scripts\activate
